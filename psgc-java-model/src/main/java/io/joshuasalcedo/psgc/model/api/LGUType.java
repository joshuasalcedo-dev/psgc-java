package io.joshuasalcedo.psgc.model.api;

// 2. LGU Type Enum (to distinguish between different types)
public enum LGUType {
    REGION,
    PROVINCE,
    CITY,
    MUNICIPALITY,
    BARANGAY,
    DISTRICT,
    SUB_MUNICIPALITY
}
